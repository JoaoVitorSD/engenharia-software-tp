spring:
  application:
    name: gerenciamento
  h2:
    console:
      enabled: true
  datasource:
      url: jdbc:h2:mem:gerenciamento
      username: sa
      password: sa
      driver-class-name: org.h2.Driver
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
