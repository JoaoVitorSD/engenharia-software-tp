spring:
  application:
    name: gerenciamento-pessoas
  h2:
    console:
      enabled: true
  datasource:
      url: ${DB_URL}
      username: ${DB_USER}
      password: ${DB_PASS}
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true